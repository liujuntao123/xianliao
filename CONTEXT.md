# 闲记（XianJi）

单用户自托管的待办与速记应用：简单的多清单任务管理（含一层子任务）与三栏式快捷笔记，数据全部保存在用户自己的飞书多维表格中。

## Language

**闲记**:
本应用的产品名（谐音「闲记/记一下」）。仓库名保持 `xianliao`。
_Avoid_: 贤聊（旧项目名，已被替换）

**清单**:
任务的容器，用户可自建多个（如「工作」「生活」），左侧栏切换。清单只有名称。
_Avoid_: 列表、文件夹、项目

**任务**:
一条待办事项。属性：标题、描述（可选多行）、完成状态、截止日期（可选）、标签（0..n）、所属清单（恰好一个）。
_Avoid_: 待办、TODO 项

**子任务**:
任务的下一级拆解项，恰好一层，不可再嵌套。属性：标题、完成状态、所属任务。全部勾完不自动完成父任务。
_Avoid_: 子项、检查项、清单项

**标签**:
任务与快捷笔记共用的分类词，即字符串（多维表格多选字段选项名）。输入新名称即创建，按名称去重与共享；展示色由名称哈希在本地稳定生成。
_Avoid_: 分类、类别、topic

**快捷笔记**:
独立于任务体系的速记条目。属性：标题、正文（多行纯文本）、标签。三栏式交互：中栏列表、右栏详情随时编辑并自动保存；按修改时间倒序。
_Avoid_: 备忘、便签、memo

**全部视图**:
跨所有清单的任务聚合视图，尾部附加速记区（点击笔记同样打开详情面板）。v1 不做「今天 / 最近 7 天」等日期智能清单。

**新建入口**:
创建任务 / 笔记的交互入口。移动端：右下角悬浮按钮（FAB）→ 底部抽屉选择类型。桌面端：任务视图顶部的快速添加行、「快捷笔记」顶部的捕捉框，以及「全部」视图速记区的捕捉框（与任务快速添加构成该页两个新建入口）。
_Avoid_: 浮层按钮、加号按钮

**详情面板**:
右栏常驻（桌面）或从下往上的底部抽屉（移动端），承载任务详情与笔记详情；删除等危险操作收在 Header 右上角的收起菜单里。
_Avoid_: 弹窗、模态框

**Access Key**:
部署者在环境变量中设置的唯一访问口令。前端输入后随每次请求携带，后端校验。单用户，无用户体系。
_Avoid_: 密码、token、API Key

**首启向导**:
首次进入且缺少飞书配置时的引导流程：扫码一键创建飞书智能体应用 → 自动创建 Base 与数据表 → 引导将凭证写入环境变量并重新部署。

**Base**:
本应用唯一的持久化存储，即用户飞书账号下的一个多维表格文档，由首启向导自动创建，并以可管理权限共享给扫码用户。
_Avoid_: 数据库、表格（指单个数据表时）、Bitable

## 排序与并发

- 任务固定排序：未完成在前 → 截止日期升序（无日期最后）→ 创建时间。无手动排序。
- 多端并发编辑冲突：最后写入胜（以记录修改时间为准）。

## 归属

- 所有数据持久化于飞书 Base；应用自身零本地持久化，唯一状态是环境变量。
